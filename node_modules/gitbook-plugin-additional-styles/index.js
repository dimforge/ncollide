module.exports = {
    book: {
        assets: "./book",
        css: [
            "additional_styles.css"
        ]
    },
    hooks: {
        "page:after": function(page) {
            // Those are ugly.
            // But I do not know a better way.
            page.content = page.content.replace("gitbook/images/favicon.ico", "./img/favicon.png");
            page.content = page.content.replace("gitbook/images/apple-touch-icon-precomposed-152.png", "./img/favicon.ico");

            return page;
        }
    },
};
